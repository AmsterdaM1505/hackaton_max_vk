# ⚡ Быстрый старт - Бот для знакомств

## 30 секунд до запуска

### 1️⃣ Установи зависимости
```bash
pip install -r requirements.txt
```

### 2️⃣ Добавь токен
```bash
cp .env.example .env
# Отредактируй .env и вставь BOT_TOKEN
```

**Или прямо в код:**
```bash
# Отредактируй config.py:
BOT_TOKEN = 'твой_токен_из_MAX'
```

### 3️⃣ Запусти бота
```bash
python main.py
```

✅ Готово! Бот слушает сообщения.

---

## Где получить токен?

1. Откройте мессенджер **MAX**
2. Найдите чат **MasterBot**
3. Отправьте: `/create`
4. Укажите имя и ник для бота
5. Скопируйте полученный токен

---

## Первые команды для тестирования

| Команда | Описание |
|---------|---------|
| `/start` | Начать, создать профиль |
| `/menu` | Главное меню |
| `/view_profile` | Мой профиль |
| `/browse` | Просмотр анкет |
| `/likes` | Мои мэтчи |
| `/edit` | Редактировать профиль |

---

## Структура папки

```
project/
├── main.py               ← Запускай это
├── config.py             ← Вставь токен сюда
├── database.py           ← БД (SQLite)
├── handlers.py           ← Логика бота
├── .env.example          ← Скопируй в .env
├── requirements.txt      ← Зависимости
└── README.md             ← Полная документация
```

---

## Популярные ошибки

### ❌ "Токен не установлен"
**Решение:** Открой `config.py` и замени `'ваш_токен_здесь'` на реальный токен

### ❌ "ModuleNotFoundError: No module named 'maxapi'"
**Решение:** Установи зависимости
```bash
pip install maxapi python-dotenv
```

### ❌ "Error: cannot open database file"
**Решение:** Проверь права доступа к папке
```bash
chmod 755 /app/rkuzmin/project/
```

---

## Что дальше?

1. **Полная документация**: `README.md`
2. **Тестовые сценарии**: `test_scenarios.md`
3. **Идеи расширений**: `features_roadmap.md`

---

## Быстрый чеклист

- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Токен вставлен в `config.py` или `.env`
- [ ] Бот запущен (`python main.py`)
- [ ] Видно сообщение "👂 Бот слушает входящие сообщения..."
- [ ] Можешь написать боту `/start` в MAX

---

**Готово! 🎉**

Теперь открой **MAX**, найди своего бота и напиши `/start`

---

*Версия 1.0 | Платформа MAX | Python 3.8+*
