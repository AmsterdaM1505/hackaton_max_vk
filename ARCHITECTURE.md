# 🏗️ Архитектура бота для знакомств

## Общая архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                    MAX Мессенджер                           │
│  (Платформа для общения, где работает наш бот)             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ Сообщения пользователя
                     │ (MessageCreated Event)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    maxapi Library                           │
│  (Библиотека для работы с MAX API)                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ Диспетчеризация событий
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    Dispatcher                               │
│  (Маршрутизирует события на обработчики)                    │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
    @message_   @message_    @message_
     handler1    handler2    handlerN
        │            │            │
        └────────────┼────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │   DatingBotHandlers        │
        │  (Основная логика бота)    │
        └────────┬───────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
    Database         Utils & Keyboards
    (SQLite)       (Валидация, форматирование)
        │
        ▼
    dating_bot.db
    ├── users
    ├── likes
    ├── dislikes
    ├── messages
    └── user_states
```

---

## Поток данных пользователя (Data Flow)

### 1. Создание профиля

```
START /start
  │
  ├─► BOT: "Как тебя зовут?"
  │
  ├─► USER: "Александр"
  │   ├─► validate_name("Александр") ✓
  │   ├─► set_user_state(user_id, ENTER_NAME)
  │
  ├─► BOT: "Сколько тебе лет?"
  │
  ├─► USER: "25"
  │   ├─► validate_age("25") ✓
  │   ├─► set_user_state(user_id, ENTER_AGE, {name: "Александр", age: 25})
  │
  ├─► BOT: "Выбери пол"
  │   ├─► get_gender_keyboard()
  │
  ├─► USER: "/gender_male"
  │   ├─► set_user_state(user_id, ENTER_BIO, {..., gender: "male"})
  │
  ├─► BOT: "Расскажи о себе"
  │
  ├─► USER: "Люблю путешествия..."
  │   ├─► validate_bio(text) ✓
  │   ├─► set_user_state(user_id, CHOOSE_CATEGORIES)
  │
  ├─► BOT: "Выбери категории"
  │   ├─► get_categories_keyboard()
  │
  ├─► USER: "/love"
  │   ├─► categories = ["love"]
  │   ├─► set_user_state(user_id, CHOOSE_CATEGORIES, {..., categories})
  │
  ├─► USER: "/done_categories"
  │   ├─► create_user(user_id, name, age, gender, bio, categories)
  │   ├─► save to DB
  │   ├─► set_user_state(user_id, MAIN_MENU)
  │
  └─► BOT: "✅ Профиль создан!" + get_main_menu_keyboard()
```

### 2. Просмотр анкет (Swipe)

```
/browse
  │
  ├─► BOT: "Выбери категорию"
  │   ├─► get_browse_category_keyboard()
  │
  ├─► USER: "/love"
  │   ├─► get_profile_for_user(user_id, "love")
  │   │   └─► Query DB: SELECT * FROM users WHERE category="love" AND not viewed
  │   ├─► set_user_state(user_id, VIEWING_PROFILE, {current_profile: {...}})
  │
  ├─► BOT: (показывает карточку)
  │   ├─► format_profile_card(profile)
  │   └─► get_profile_view_keyboard()
  │
  ├─► USER: "/like"
  │   ├─► add_like(user_id, profile_id)
  │   ├─► INSERT likes (user_from, user_to) VALUES (...)
  │   ├─► check_match: has_interacted(profile_id, user_id)?
  │   │   └─► if YES → MATCH! 💕
  │   ├─► show_next_profile(category)
  │
  └─► (повтор для следующей анкеты)
```

### 3. Система мэтчей

```
MATCH DETECTION:

User1 /like → User2
  ├─► add_like(user1, user2)
  │
  └─► Check: Does user2 like user1?
      ├─► Query: SELECT 1 FROM likes WHERE user_from=user2 AND user_to=user1
      │
      ├─► if EXISTS:
      │   ├─► MATCH! 💕💕
      │   ├─► notify_user1("МЭТЧ!")
      │   └─► notify_user2("МЭТЧ!")
      │
      └─► if NOT:
          └─► (ждём)
```

### 4. Чат между пользователями

```
/messages
  │
  ├─► get_matches(user_id)
  │   └─► SELECT user_to FROM likes WHERE user_from=user_id
  │       AND user_to IN (SELECT user_from FROM likes WHERE user_to=user_id)
  │
  ├─► BOT: "Твои мэтчи:" + список
  │
  ├─► USER: "/chat_maria"
  │   ├─► extract_match_from_command("maria")
  │   ├─► set_user_state(user_id, IN_CHAT, {match_id: "maria"})
  │
  ├─► USER: "Привет! 👋"
  │   ├─► save_message(user_id, match_id, "Привет! 👋")
  │   ├─► INSERT messages (from_user, to_user, message, created_at)
  │
  └─► BOT: "✅ Сообщение отправлено!"
```

---

## Компонентная архитектура

### 1. **config.py** - Конфигурация
```python
┌─────────────────────┐
│    config.py        │
├─────────────────────┤
│ BOT_TOKEN           │ ← Токен из MAX
│ DATABASE_PATH       │ ← Путь к БД
│ CATEGORIES          │ ← Типы знакомств
│ MIN_AGE, MAX_AGE    │ ← Ограничения
│ MESSAGES            │ ← Тексты сообщений
│ INLINE_BUTTONS      │ ← Названия кнопок
└─────────────────────┘
```

### 2. **database.py** - Слой доступа к данным
```python
┌──────────────────────────┐
│    Database Class        │
├──────────────────────────┤
│ CRUD операции:           │
│ ├─ create_user()         │
│ ├─ get_user()            │
│ ├─ update_user()         │
│ ├─ add_like()            │
│ ├─ add_dislike()         │
│ ├─ get_matches()         │
│ ├─ get_profile_for_user()│
│ ├─ save_message()        │
│ ├─ set_user_state()      │
│ └─ get_user_state()      │
└──────────────────────────┘
```

### 3. **states.py** - Конечный автомат (FSM)
```python
┌──────────────────────────┐
│   UserState (Enum)       │
├──────────────────────────┤
│ START                    │
│ MAIN_MENU               │
│ ENTER_NAME              │
│ ENTER_AGE               │
│ ENTER_GENDER            │
│ ENTER_BIO               │
│ CHOOSE_CATEGORIES       │
│ BROWSE_PROFILES         │
│ VIEWING_PROFILE         │
│ IN_CHAT                 │
│ EDIT_PROFILE            │
└──────────────────────────┘
```

### 4. **handlers.py** - Основная логика
```python
┌──────────────────────────────┐
│  DatingBotHandlers           │
├──────────────────────────────┤
│ Обработчики:                 │
│ ├─ cmd_start()               │
│ ├─ cmd_browse()              │
│ ├─ cmd_like()                │
│ ├─ cmd_dislike()             │
│ ├─ cmd_likes()               │
│ ├─ handle_text_input()       │
│ ├─ handle_name_input()       │
│ ├─ handle_age_input()        │
│ ├─ handle_bio_input()        │
│ └─ _show_next_profile()      │
└──────────────────────────────┘
```

### 5. **keyboards.py** - Форматирование UI
```python
┌──────────────────────────────┐
│   Keyboard Functions         │
├──────────────────────────────┤
│ ├─ get_main_menu_keyboard()  │
│ ├─ get_gender_keyboard()     │
│ ├─ get_categories_keyboard() │
│ ├─ format_profile_card()     │
│ ├─ get_profile_view_keyboard()
│ └─ format_matches_list()     │
└──────────────────────────────┘
```

### 6. **utils.py** - Утилиты
```python
┌──────────────────────────┐
│   Utils Functions        │
├──────────────────────────┤
│ Валидация:               │
│ ├─ validate_name()       │
│ ├─ validate_age()        │
│ ├─ validate_bio()        │
│ └─ validate_gender()     │
│                          │
│ Помощники:               │
│ ├─ extract_command_arg() │
│ ├─ format_user_profile() │
│ └─ get_gender_emoji()    │
└──────────────────────────┘
```

---

## Диаграмма состояний (FSM)

```
                    START
                      │
                      ▼
            ┌─────────────────┐
            │  ENTER_NAME     │
            │ "Как тебя зовут?"│
            └─────────┬───────┘
                      │
                      ▼
            ┌─────────────────┐
            │  ENTER_AGE      │
            │ "Сколько лет?"  │
            └─────────┬───────┘
                      │
                      ▼
            ┌─────────────────┐
            │ ENTER_GENDER    │
            │ "Выбери пол"    │
            └─────────┬───────┘
                      │
                      ▼
            ┌─────────────────┐
            │  ENTER_BIO      │
            │ "О себе"        │
            └─────────┬───────┘
                      │
                      ▼
          ┌───────────────────────┐
          │  CHOOSE_CATEGORIES    │
          │  "Выбери категории"   │
          └───────────┬───────────┘
                      │
                      ▼
          ┌────────────────────────┐
          │    MAIN_MENU           │
          │ 📋 Главное меню        │
          └─────────┬──────────────┘
         /          │                  \
        /           │                   \
       ▼            ▼                    ▼
  VIEW_PROFILE   BROWSE_PROFILES    EDIT_PROFILE
       │            │                    │
       │            ▼                    │
       │   CHOOSE_CATEGORY              │
       │            │                    │
       │            ▼                    │
       │   VIEWING_PROFILE              │
       │            │                    │
       │     ┌──────┼──────┐             │
       │     ▼      ▼      ▼             │
       │   LIKE  DISLIKE  SKIP     [EDIT_FIELD]
       │     │      │      │             │
       │     └──────┴──────┘             │
       │            │                    │
       └────────────┴────────────────────┘
              │
              ▼
           MAIN_MENU
```

---

## Диаграмма БД

```
users                          likes
┌─────────────────────┐        ┌──────────────────┐
│ user_id (PK)        │        │ id (PK)          │
│ username            │        │ user_from (FK)   │◄─────┐
│ name                │        │ user_to (FK)     │      │
│ age                 │        │ created_at       │      │
│ gender              │        └──────────────────┘      │
│ bio                 │              △                   │
│ categories (JSON)   │              │ (1 to M)          │
│ created_at          │              │                   │
│ updated_at          │┌─────────────┴────────────────┐  │
└─────────────────────┤                                │  │
         △            │      MATCHES                  │  │
         │            │   (Both liked)                │  │
         │            │                               │  │
         │            └──────────────────────────────┘  │
         │                                              │
         │  dislikes              messages             │
         │  ┌──────────────────┐  ┌─────────────────┐ │
         │  │ id (PK)          │  │ id (PK)         │ │
         │  │ user_from (FK)   │  │ from_user (FK)  ├─┘
         │  │ user_to (FK)     │  │ to_user (FK)    ├─┐
         │  │ created_at       │  │ message         │ │
         │  └──────────────────┘  │ created_at      │ │
         │                        │ is_read         │ │
         │                        └─────────────────┘ │
         └────────────────────────────────────────────┘

user_states
┌─────────────────────────┐
│ user_id (PK, FK)        │
│ state (FSM state)       │
│ data (JSON context)     │
│ updated_at              │
└─────────────────────────┘
```

---

## Поток обработки события

```
┌─────────────────────────────────┐
│   Message from MAX Messenger    │
│  (e.g., "/like")                │
└────────────┬────────────────────┘
             │
             ▼
        ┌────────────────────┐
        │  maxapi Receives   │
        │  MessageCreated    │
        │  Event             │
        └────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │   Dispatcher         │
      │  Checks filters      │
      │  F.message.body.text │
      └────────┬─────────────┘
               │
               ▼
    ┌────────────────────────┐
    │  Find matching handler │
    │  (e.g., @message_)     │
    │  created for "/like"   │
    └────────┬───────────────┘
             │
             ▼
  ┌──────────────────────────┐
  │  DatingBotHandlers       │
  │  execute handler         │
  │  cmd_like(event)         │
  └────────┬─────────────────┘
           │
      ┌────┴────┬────────────┐
      ▼         ▼            ▼
   Extract   Query DB    Check State
   user_id   (get FSM)   (VIEWING_PROFILE)
      │         │            │
      └────┬────┴────────────┘
           │
           ▼
    ┌────────────────────────┐
    │  Business Logic        │
    │  - Validate state      │
    │  - Add like to DB      │
    │  - Check for match     │
    │  - Get next profile    │
    └────────┬───────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  Format Response         │
    │  (keyboards.py)          │
    │  - Profile card          │
    │  - Menu buttons          │
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  Send via Bot API        │
    │  await event.message     │
    │  .answer(message)        │
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  Message delivered to    │
    │  user in MAX Messenger   │
    └──────────────────────────┘
```

---

## Интеграция компонентов

```
┌──────────────────────────────────────────────────┐
│                   main.py                        │
│  (Инициализация и запуск)                        │
└────────────┬─────────────────────────────────────┘
             │
      ┌──────┴────────┐
      ▼               ▼
   Bot()          Dispatcher()
      │               │
      │               ▼
      │        DatingBotHandlers(dp, bot)
      │               │
      │        ┌──────┴──────────────┐
      │        │                     │
      │        ▼                     ▼
      │    register_handlers()   @message_created
      │        │                 decorators
      │        │
      │        ├─► config.py
      │        ├─► database.py
      │        ├─► keyboards.py
      │        ├─► utils.py
      │        └─► states.py
      │
      └─► asyncio.run(main())
           │
           ▼
      dp.start_polling(bot)
           │
           ▼
   Ожидание сообщений в long-polling
```

---

## Жизненный цикл приложения

```
START
  │
  ├─► Читай config (токен, константы)
  │
  ├─► Инициализируй БД
  │   └─► Создай таблицы если их нет
  │
  ├─► Создай Bot(token)
  │
  ├─► Создай Dispatcher()
  │
  ├─► Зарегистрируй обработчики
  │   └─► Подпишись на события сообщений
  │
  ├─► Запусти long-polling
  │   │
  │   └─► Слушай входящие сообщения в цикле
  │       │
  │       ├─► Получи MessageCreated event
  │       │
  │       ├─► Распредели к обработчику
  │       │
  │       ├─► Выполни логику
  │       │
  │       ├─► Отправь ответ пользователю
  │       │
  │       └─► (back to слушай)
  │
  └─► (CTRL+C) → Остановка
```

---

## Масштабируемость (TODO)

### Текущее:
- SQLite (подходит для <10k пользователей)
- Single-thread long polling
- In-memory состояния

### Будущее:
```
PostgreSQL ├─► Масштабируемость БД
           ├─► Репликация
           └─► Бэкапы

Redis      ├─► Кэширование профилей
           ├─► Сессии пользователя
           └─► Real-time уведомления

Microservices
           ├─► Auth Service
           ├─► Profile Service
           ├─► Matching Service
           └─► Messaging Service

Async      ├─► WebSockets вместо long-polling
           └─► Уведомления в реальном времени
```

---

**Последнее обновление:** 2025-11-08
